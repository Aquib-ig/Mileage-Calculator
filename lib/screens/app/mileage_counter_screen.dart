import 'package:flutter/material.dart';

class MileageCounterScreen extends StatelessWidget {
  const MileageCounterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Check your mileage here"),),
    );
  }
}